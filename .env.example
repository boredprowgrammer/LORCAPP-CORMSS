# Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Environment (development or production)
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_NAME=church_officers_db
DB_USER=root
DB_PASS=

# Application URL
BASE_URL=http://localhost/CORegistry and CORTracker

# Encryption Keys
# Generate strong keys using: openssl rand -base64 32
# NEVER use these example values in production!
MASTER_KEY=
ENCRYPTION_KEY=

# Gemini AI API Key (for intelligent family member suggestions)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=
